import sys


def simpleArraySum(n, ar):
    total = 0
    for val in ar:
        total += val
    return total


n = int(input().strip())
ar = list(map(int, input().strip().split(' ')))
result = simpleArraySum(n, ar)
print(result)
